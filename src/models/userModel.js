class userModel {
    constructor(user) {
        this.nama = user.nama;
        this.username = user.username;
        this.password = user.password;
        this.role = user.role;
    }
}

export default userModel;